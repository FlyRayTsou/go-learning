#! /bin/bash

# When set -x is active, the shell will print each command it executes 
set -x
# When set -e is active, the script will immediately exit if any command exits with a non-zero status (i.e., an error occurs). 
set -e

echo "hello world"